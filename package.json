{
  "name": "flowboard",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=@flowboard/web",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "start": "npm run start --workspace=@flowboard/web"
  },
  "devDependencies": {
    "turbo": "^2.5.5"
  },
  "packageManager": "npm@11.12.1"
}

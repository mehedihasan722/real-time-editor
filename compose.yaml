services:
  web:
    build: .
    env_file:
      - .env.local
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
      - next_cache:/app/apps/web/.next
    command: npm run dev

volumes:
  node_modules:
  next_cache:
